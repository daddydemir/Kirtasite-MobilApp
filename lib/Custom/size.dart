import 'package:flutter/material.dart';

class CustomSize{

  final paddingHorizontal = const EdgeInsets.symmetric(horizontal:20);
  final paddingAllx8 = const EdgeInsets.all(8.0);
  final padingOnlyBottomx13 = const EdgeInsets.only(bottom:13.0);
  final bottomNavBar = const EdgeInsets.only(left:50, right:50, bottom:10);

  final double sigma = 8.0;
}